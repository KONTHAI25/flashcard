# No environment variables are required. This app is local-first: decks and
# review schedules live in browser localStorage; there is no database.
#
# FUTURE ONLY (unused — no code reads process.env today): if server-side
# features are ever added, configure them here, e.g.
# DATABASE_URL="postgresql://user:password@example.neon.tech/flashcards?sslmode=require"
